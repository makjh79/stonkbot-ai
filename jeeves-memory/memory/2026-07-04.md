# Memory — July 4, 2026

## Memory sync check + Einstein handover study
- Howie asked for a fresh memory sync check and to study Einstein's latest StonkBOT handover.
- Pulled `/opt/stonk-ai/agent-messages/jeeves/handover-from-einstein-20260704.md` from VPS via SCP; saved locally.
- **Pulled Einstein's canonical MEMORY.md and daily raw log** from `/root/.openclaw/workspace/` on VPS and reconciled missing operational sections.
- Updated `MEMORY.md` with July 1–4 changes: 10 confirmation chips end-to-end, PEAD fully removed, tier alignment, July 2 bot fixes (IV dict bug, thesis-exit churn, margin guard, watchlist cap), July 1 monitor heartbeat + risk cap loosening, and July 4 Einstein cleanup.
- **Merged missing 2026-07-02 sections from Einstein's canonical memory:**
  - Alpha config patches (12% STRONG_NOW cap, readiness multipliers 3.0×/1.5×/0.5×/blocked, top-12 entry cap, 5-day min hold, -5% hard cut, cash gate, RISK_OFF STRONG_NOW-only, 5s refresh).
  - Bot aligned to watchlist (only top 20 symbols for entries).
  - CRWD stock-split auto-detection + `split_guardian.py`.
  - Duplicate-sell / 403 loop cleanup + alert-cron removal.
- **Einstein consistency corrections (Jul 4 PM):** applied latest canonical values — bot version v2.5, readiness weights now 10-factor (signal 20%, sector 30%, EMA 12%, RSI 10%, intraday 10%, MACD 8%, volume 5%, options IV 5%, relative volume 5%, VWAP 5%), position caps 12% STRONG_NOW / 8% others, sizing multipliers 3.0×/1.5×/0.5×/blocked by readiness band.
- Updated agent communication notes: Einstein's workspace is at `/root/.openclaw/workspace/` on VPS; Jeeves is the VPS/Ollama primary bot; memory message relay layout is `/opt/stonk-ai/agent-messages/{einstein,jeeves}/`.
- Added new lessons learned: numeric-score exclusion, canonical paths, scheduler status files, case-insensitive mode matching, stub removal, cash-only guard placement, IV migration consumers, thesis-exit minimum hold, broken stubs surviving in production.
- Added backup entry: `/opt/stonk-ai/backups/2026-07-04-einstein/`.

## Watchlist Refresh Final Fix (Jul 4 ~01:45 UTC)
- **User report:** watchlist table refresh still not working.
- **Root cause:** previous path chained through `getWatchlistWithLivePrices()` → `buildWatchlistFromLiveData()` → heavy text generation → `updateWatchlistDOM()`, which was unreliable (stale data, cache miss, complex timing).
- **Fix:** replaced entire watchlist interval with `_updateWatchlistTableDirectly()`:
  - Reads raw `aiWatchlistPrices` directly
  - Walks existing DOM rows by `data-symbol`
  - Patches only price and change% cells — no text generation, no sorting
- **Interim bug:** stray `+` syntax error in `build_price_badge_combined()` broke entire JS; restored from `index.html.bak-20260704-110525`, redeployed as v105.
- **Current refresh cadence:** inline watchlist 15s, modal/standalone 15s, popup detail 15s, backend deploy every 15s.

## Session Status Summary (Jul 4, 2026)
| System | Status |
|---|---|
| stonk-ai.service | v2.5, PAPER, running |
| stonk-ai-data.service | running |
| Readiness weights | Normalized (100%) |
| Per-factor breakdown | Live in `signals.json` |
| -5% hard cut | Live in `risk_engine.py` |
| Avg-in cash gate | Live in `trading_bot.py` |
| Narrative generator | Throttled to 60 min during market hours |
| Market status API | Live on `/api/market-status` |
| Frontend timers | Deduped (21 → 16 setIntervals) |
| Watchlist text cache | Live (strategy/catalyst/risk) |

## Watchlist UI Cleanup (Jul 4 19:38 HKT)
- **User report:** APP showed as a “Next Buy” despite being in BUILDING tier; AI Score column was confusing.
- **Root cause:** `dynamic_watchlist_manager.py` had a legacy `diversification` branch that marked non-PRIME symbols as buy candidates when they passed readiness/confirms/sector thresholds. Frontend rendered them under “Next Buys”.
- **Fix:**
  - Removed the `diversification` candidate branch from `dynamic_watchlist_manager.py`
  - Removed the 🌿 Diversification block from the frontend “Next Buys” section
  - Replaced the watchlist table “AI” column with a “Conf” (confirmations /10) column
  - Regenerated `ai_watchlist_live.json`; `buy_candidates` now only contains `queued`, `add`, `hold`, `not_ready`, and `high_beta_blocked`
  - Bumped `index.html` cache buster to `stonkbot_v152`
- **Result:** Only PRIME queued/add candidates appear as “Next Buys”. BUILDING/WATCHING/TRACKING symbols no longer show there.

## Confirmation Count Mismatch Fix (Jul 4 20:03–20:53 HKT)
- **User report:** UBER showed 6/10 confirmations in the watchlist table but 5/10 in the popup; SNOW showed 7 green chips but 6/10 in the header.
- **Root cause:** `buildFactorChips()` in `index.html` was computing the popup header count as `factors.filter(f => f.ok).length` (green chips only), while the watchlist table and backend used the canonical `computeCanonicalConfirmationCount()` logic. Additionally, the RSI chip only turned green for `bullish`, but the backend counts `neutral` and `oversold` too. MOM chip logic also drifted from backend.
- **Fix:**
  - Changed the popup factor header to use the passed `count` parameter
  - Changed the RSI chip `ok` rule to include `neutral` and `oversold` (matching backend)
  - Patched `readiness_score.py` to count `momentum_score >= 50` as a confirmation
  - Patched `dynamic_watchlist_manager.py` to recompute `confirmation_count` from `confirmations` instead of copying stale value
  - Patched frontend `computeCanonicalConfirmationCount()` to also count `momentum_score >= 50` so it matches backend
  - Updated stale tooltip/fallback text references from `≥75 + 4+ confirmations` to `≥77 + 5+ confirmations`
  - Bumped cache buster to `stonkbot_v156`
- **Verification:** all 20 watchlist symbols now match between server `confirmation_count`, green chips, and popup header. No mismatches.

## Tier Display Fix (Jul 4 22:04 HKT)
- **User report:** tiers were showing backend names (`STRONG_NOW`, `NOW`) instead of display names (`PRIME`, `BUILDING`).
- **Root cause:** `index.html` watchlist builder was setting `display_tier = data.signal_tier` instead of using `data.display_tier`.
- **Fix:**
  - Changed watchlist builder to prefer `data.display_tier` over `data.signal_tier`
  - Updated `effectiveTier` and row styling to use the display tier
  - Updated popup tier chip to use display tier
  - Bumped cache buster to `stonkbot_v164`

## Conf Badge Color Alignment (Jul 4 22:17 HKT)
- **User report:** confirmation count badge colors were not aligned to tier colors.
- **Fix:** Conf badge color now matches tier badge color:
  - PRIME → cyan
  - BUILDING → green
  - WATCHING → amber
  - TRACKING → gray
- Cache buster bumped to `stonkbot_v165`.

## End-to-End Pipeline Alignment (Jul 4 22:21–23:19 HKT)
- **User request:** verify trading bot, watchlist strategy, holdings, and watchlist popups are all aligned with Alpaca.
- **Fixes:**
  - Patched `signal_engine.py` to recompute `confirmation_count` from `confirmations` before saving
  - Patched `dynamic_watchlist_manager.py` to recompute `confirmation_count`
  - Patched `fetch_ai_watchlist.py` to recompute `confirmation_count`
  - Regenerated `signals.json`, `ai_watchlist_live.json`, `popup_content.json`, `watchlist_narratives.json`
  - Normalized all 130 symbols via `run_signal_engine.py`
- **Result:** signals.json, watchlist, and popups all agree on confirmation counts and entry eligibility.

## StonkBOT Monitor Overhaul + Silent Alerts (Jul 4 23:19–23:46 HKT)
- **User request:** Deep overhaul of `comprehensive_monitor.py` to include audit checks + LLM narrative freshness/validity; then silence alerts unless something is broken.
- **Changes:**
  - `check_alignment_signals_vs_watchlist()`: updated for new watchlist JSON structure; added `entry_eligible`, `confirmation_count`, and tier mapping checks using readiness thresholds (PRIME ≥77 + entry, BUILDING ≥55, WATCHING ≥40, TRACKING <40).
  - Added `check_trading_bot_entry_gate()`: flags any non-PRIME or under-gated symbol marked `entry_eligible=True`.
  - Added `check_alpaca_portfolio_sync()`: cross-checks Alpaca positions vs `popup_content.json` and `portfolio_data.json`.
  - Added `check_llm_narrative_freshness_and_validity()`: validates `popup_narratives.json` and `watchlist_narratives_llm.json` have all required fields and are fresh during market hours; also checks merged `popup_content.json` and `watchlist_narratives.json` contain all LLM fields.
  - Added `WATCHING` tier to `dynamic_watchlist_manager.py` (readiness 40–55) so backend `WATCH` maps correctly.
  - Enriched `watchlist_narratives.json` with `confirmations`, `confirmation_count`, `readiness_score`, `signal_tier`, `entry_eligible` from `generate_popup_content_v3.py`.
  - Removed redundant `/9` denominator stale check in `check_html_currency`.
- **Silenced alerts:**
  - Removed dead semantic-contradiction block (false positives).
  - Made LLM narrative stale alerts market-hours only.
  - Made cron heartbeat stale alerts market-hours only (4× tolerance off-hours).
  - Downgraded missing `data-sources provenance` to silent (cosmetic).
  - Fixed tier alignment check to use readiness thresholds, removing false `WATCH → WATCHING` mismatches.
- **Deployment:** patched on VPS; backup saved to `/opt/stonk-ai/backups/20260704-2345-comprehensive/` and locally to `workspace/backups/v152/`.
- **Run result:** monitor now exits `0` with `0` issues and `0` warnings when healthy.
- **State:** Only PRIME/entry-eligible symbols: **UBER, SNOW**.

## Backups
- `/opt/stonk-ai/backups/2026-07-04-einstein/` — 14 files + manifest.
- `/opt/stonk-ai/backups/end-to-end-align-20260704-080417/` — Gemini alignment patch files.
- `/opt/stonk-ai/backups/patch-gemini-fixes-20260704-074336/` — initial patch files.
- `/opt/stonk-ai/backups/tier-align-option-b-20260704-083926/` — tier Option B files.
- `/opt/stonk-ai/backups/e2e-tier-align-20260704-111945/` — pre-watchlist-cleanup backup of `dynamic_watchlist_manager.py` and `index.html`.
- `/opt/stonk-ai/backups/20260704-2345-comprehensive/` — final comprehensive backup including monitor, watchlist manager, readiness_score, signal_engine, generate_popup_content_v3, index.html, and JSON outputs.
