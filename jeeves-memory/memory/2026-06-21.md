### Performance history scheduler revived + auto-refresh
**Root cause found:**
- `fetch_data_simple.py` (systemd `data-fetcher.service`) was dying silently every 5 min
- Death loop: Process starts → market closed → `sleep(300)` → `monitor_services.sh` sees stale data → `systemctl restart` SIGTERMs it → repeats
- Last actual data: June 19. `portfolio_history.json` frozen for 2 days. `portfolio_data.json` 3301 min stale.

**Fix applied to fetch_data_simple.py:**
- Added initial `fetch_and_save()` call at startup (before the loop), so data is always fresh regardless of market hours
- This breaks the death loop: data stays <30 min old, monitor stops restarting

**New: sync_alpaca_trades.py**
- Pulls all fills from Alpaca daily, merges with existing rationale, writes `last_updated` timestamp
- Added to crontab: `*/5 * * * * flock -n /tmp/alpaca_sync.lock python3 sync_alpaca_trades.py`
- Ensures `trades_log.json` stays in sync with broker activity

**Website JS update:**
- History panel now auto-refreshes every 2 min while open (`setInterval`), picks up new trades + portfolio checks
- Cache-busting already in place on all fetch calls (`?t=Date.now()`)

**Verification:**
- data-fetcher stable at PID 585614, portfolio_data.json 3.1 min old, trades updated
- Monitor no longer flagging staleness on recent checks
- portfolio_history.json now accumulating new entries (2 unique days: June 19 + 21)
- Deployed to stonkbot.ai
### Performance history scheduler revived + auto-refresh
**Root cause found:**
- `fetch_data_simple.py` (systemd `data-fetcher.service`) was dying silently every 5 min
- Death loop: Process starts → market closed → `sleep(300)` → `monitor_services.sh` sees stale data → `systemctl restart` SIGTERMs it → repeats
- Last actual data: June 19. `portfolio_history.json` frozen for 2 days. `portfolio_data.json` 3301 min stale.

**Fix applied to fetch_data_simple.py:**
- Added initial `fetch_and_save()` call at startup (before the loop), so data is always fresh regardless of market hours
- This breaks the death loop: data stays <30 min old, monitor stops restarting

**New: sync_alpaca_trades.py**
- Pulls all fills from Alpaca daily, merges with existing rationale, writes `last_updated` timestamp
- Added to crontab: `*/5 * * * * flock -n /tmp/alpaca_sync.lock python3 sync_alpaca_trades.py`
- Ensures `trades_log.json` stays in sync with broker activity

**Website JS update:**
- History panel now auto-refreshes every 2 min while open (`setInterval`), picks up new trades + portfolio checks
- Cache-busting already in place on all fetch calls (`?t=Date.now()`)

**Verification:**
- data-fetcher stable at PID 585614, portfolio_data.json 3.1 min old, trades updated
- Monitor no longer flagging staleness on recent checks
- portfolio_history.json now accumulating new entries (2 unique days: June 19 + 21)
- Deployed to stonkbot.ai

### Cron audit + orphan scheduling (21 June 2026 22:52)
**Removed:**
- Dead `fetch_watchlist_prices.py` cron (every 2 min, 929 errors, script doesn't exist)

**Scheduled orphans:**

| Script | Schedule (HK) | Output |
|---|---|---|
| `fetch_earnings.py` | Daily 5:00 AM | `earnings_data.json` |
| `verify_prices.py` | Daily 6:45 AM | `price_verification.json` |
| `trending_sentiment_analyzer.py` | Daily 00:15 AM | `trending_sentiment.json` |

All 17 data files now have producers on schedule. 12 active crons total.

### Session wrap-up (22:59)
- Crontab changes verified persisted after reload
- All data-fetcher services stable
- Portfolio history accumulating new data
- No outstanding critical issues