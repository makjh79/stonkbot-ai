# E2E Tier Alignment Audit — 2026-07-04

## Goal
Ensure trading bot, watchlist, popup generator, LLM narrative generator, and frontend are consistent under the new tier system: backend `STRONG_NOW/NOW/WATCH/MONITOR` → display `PRIME/BUILDING/WATCHING/TRACKING`; only `STRONG_NOW/PRIME` is tradeable; entry gate `readiness >= 77`, `confirmation_count >= 5`, `above_ema=True`.

## Files Aligned (copied back to VPS)
- `/opt/stonk-ai/readiness_score.py`
- `/opt/stonk-ai/signal_engine.py`
- `/opt/stonk-ai/mean_reversion_signal.py`
- `/opt/stonk-ai/trading_bot.py`
- `/opt/stonk-ai/dynamic_watchlist_manager.py`
- `/opt/stonk-ai/generate_popup_content_v3_full.py`
- `/opt/stonk-ai/generate_narratives_llm_batched.py`
- `/var/www/hedge-fund-website/index.html`

## Key Alignments
- `generate_narratives_llm_batched.py` now uses `display_tier` when available, falls back to `tier`.
- Holdings block prompt shows `PRIME`/`BUILDING`/`TRACKING` display tier.
- Watchlist block prompt uses `watch.display_tier` > `watch.signal_tier` > `signal.display_tier` > `signal.tier`.
- Confirmation count still uses canonical `compute_confirmation_count()` from `readiness_score.py` and is shown as `/10`.
- `llm_narrative_scheduler.py` already invokes the generator; no change needed.

## Verification Results
- All Python files `py_compile` OK.
- `dynamic_watchlist_manager.py` + `generate_popup_content_v3_full.py` ran without errors.
- `generate_narratives_llm_batched.py` completed successfully: 14 holdings / watchlist narratives generated.
- `ai_watchlist_live.json`: 20 symbols, `PRIME: 2`, `BUILDING: 18`.
  - PRIME entry-eligible: `UBER` (79.9, 6/10 conf, `entry_eligible=True`), `SNOW` (78.5, 6/10 conf, `entry_eligible=True`).
- `watchlist_narratives.json` + `watchlist_narratives_llm.json`: `UBER` → PRIME, `SNOW` → PRIME.
- `popup_content.json`: holdings show `BUILDING` / expected tiers (DDOG, DUOL, ETSY sampled).
- `index.html` aligned to use display tiers consistently.

## Backup
`/opt/stonk-ai/backups/e2e-tier-align-20260704-111945.tar.gz`

## Notes
- `mean_reversion_signal.py` remains watch-only; no `entry_eligible=True`.
- `readiness_score.py` is the canonical source for `compute_confirmation_count()` (10 factors), `assign_tier()`, and `entry_eligible`.
- `trading_bot.py` only trades `STRONG_NOW`/`PRIME`, enforces readiness >= 77 and >= 5 confirmations.
- No service restart performed.
