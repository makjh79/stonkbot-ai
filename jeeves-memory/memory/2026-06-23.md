# 2026-06-23

## StonkBOT.AI maintenance — Performance chart fix
- Performance page chart was broken because `portfolio_history.json` had been truncated to only 2026-06-22 micro-checks (500 entries, one day). Page needs ≥2 daily snapshots to draw a line.
- Root cause: `fetch_data_simple.py::update_history()` kept only the most recent 500 checks; when one day had many micro-checks, it wiped all prior days.
- Restored 19 daily snapshots from `/opt/stonk-ai/backups/portfolio_history_20260622_020001.json`, merged today's live data + SPY benchmark.
- Patched `fetch_data_simple.py` to keep one snapshot per day (closest to noon UTC) and a rolling 180-day window.
- Restarted `data-fetcher.service` with the fixed script.
- Current state: 19 daily snapshots; bot -0.79% vs S&P 500 -1.64% (bot ahead).

## Safeguards added
- History-length health check in `health_check.py`: flags DEGRADED if `portfolio_history.json` drops below 7 unique days. Runs every 5 min via cron.
- Updated `/var/www/hedge-fund-website/deploy.sh` to copy `performance.html`, `fetch_data_simple.py`, and `health_check.py` from repo and restart services.
- Added nightly reconstruct cron at 10:30 PM UTC (6:30 AM HK) running `/opt/stonk-ai/reconstruct_portfolio_history.py` with `flock` lock file. Self-heals corrupted/truncated history.

## Watchlist popup sentiment
- Sentiment files were ~57 hours stale because hourly cron was commented out. Re-enabled at `*/15 * * * *` (15-minute refresh).
- Rewrote `generate_sentiment.py` to use two sources: Finnhub News + Alpaca News API, merge/dedupe articles, score with same lexicon. Output contract unchanged.
- Some tickers (SQ, ASAN) still get no coverage from either source and fall back to neutral.

## Emoji cleanup / UI polish
- Audited `index.html` emoji usage (215 occurrences, 58 unique). Removed meme/decorative emojis from FAQ, headings, brand label, PL badges, and modals.
- Desktop nav: changed Performance from `📊` to `📈` to match mobile.
- Replaced removed emojis in "More" menu with inline SVG line icons (pie-chart, trending-up, clipboard-list, help-circle).
- Remaining emoji cleanup opportunities: mobile bottom nav, watchlist tier badges, trade action badges, saved-star states.

## Tooling
- Installed Playwright on server (VPS) for automated screenshots and visual testing.
- Committed and pushed all changes to `makjh79/stonkbot-ai`.
- Server deploy script now syncs the new/updated files on next deploy.

## Notes / follow-up
- Consider replacing remaining emojis in mobile nav and watchlist with SVG line icons for a cleaner fintech look.
- The nightly reconstruct cron will run for the first time tonight; verify it preserves enough history and does not clobber today's live micro-checks.


## StonkBOT.AI navigation & Overview redesign (evening session — rolled back)
- Attempted a tabbed single-page redesign: sticky tabs (Overview / Holdings / Watchlist / About), unified header bar, cleaner Overview card, inline watchlist tab, removed save feature, unified naming to "Holdings".
- Changes looked correct in local Chromium screenshots, but rendered broken on user's actual mobile browser (tabs missing / layout off).
- Rolled back to backup at `/var/www/hedge-fund-website/index.html.latest-backup` (v42). Site restored to working state.
- Lesson: large HTML structural changes via string replacement are risky — can silently swallow adjacent elements (tab bar was accidentally removed in one replacement). Need to verify DOM structure after each big change, test on real devices early, and make changes in smaller chunks with backups.
- SVG audit noted but not implemented: 48 inline SVGs, 17 unique; biggest duplicates are hamburger menu (16x), clipboard (6x), trend-line (5x). Defer to future cleanup pass.


## StonkBOT.AI redesign continuation (late evening)
- User requested the full navigational redesign be re-implemented, but step-by-step with real-device testing.
- Started from rolled-back v42 state.
- v50: added new unified `site-header-bar` while keeping old nav elements for testing. Confirmed working on user's mobile browser.
- v51: removed old `site-header`, desktop nav, and mobile bottom nav; kept only new header bar + sticky tabs. Awaiting user confirmation on device.
- Cache buster progression: v50 → v51.
- Lesson: building the redesign incrementally (add header bar → test → remove old nav → test) avoids the sudden breakage seen with the earlier all-at-once v43-v49 attempt.
