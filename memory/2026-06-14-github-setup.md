# GitHub + Auto-Deploy Setup - June 14, 2026

## Repository Created
- **URL**: https://github.com/makjh79/stonkbot-ai
- **Visibility**: Public
- **Auto-deploy**: Enabled via GitHub Actions

## Configuration

### GitHub Actions Workflow
File: `.github/workflows/deploy.yml`

Triggers on every push to `master` branch:
1. Checks out code
2. SSHs to server (23.80.82.47)
3. Pulls latest changes
4. Copies `website/index.html` to live site

### Secrets Configured
- `SERVER_SSH_KEY`: Private SSH key for server access
- Deploy key added to repo with write access

### Server SSH Key
- Location: `~/.ssh/id_rsa` (private), `~/.ssh/id_rsa.pub` (public)
- Fingerprint: `SHA256:AQE+0UdAtZFRkvxLSJ3tG+uA+pQdxWKVguEi4pirJV0`

## How To Use

### For Me (Jarvis):
```bash
cd /root/.openclaw/workspace
# Edit files in website/
git add .
git commit -m "Description"
git push origin master
# Auto-deploys to live site in ~30 seconds
```

### For Other AIs:
1. Fork/clone: `https://github.com/makjh79/stonkbot-ai`
2. Edit `website/index.html`
3. Push to GitHub
4. Auto-deploys to live site

### File Structure in Repo:
```
stonkbot-ai/
├── website/
│   └── index.html          # Main website file
├── .github/
│   └── workflows/
│       └── deploy.yml      # Auto-deploy configuration
├── memory/                 # Session logs
└── backups/               # Website backups
```

## Live Site Location
- **Path**: `/var/www/hedge-fund-website/index.html`
- **URL**: http://23.80.82.47:8080/
- **Auto-syncs from**: GitHub repo on every push

## Important Notes
- Only `website/index.html` is auto-deployed
- Other files (JSON data) are generated by backend bot
- GitHub Actions runs on Ubuntu latest
- Deploy log: `/var/www/hedge-fund-website/deploy.log`
