# 2026-07-01 — LLM popup narrative generator moved to VPS

## Completed
- ✅ VPS v6 merge+deploy timer (`stonk-ai-popup-v6.timer`) runs every 2 minutes and merges base v2 popup data with LLM narratives.
- ✅ Batched LLM generator deployed at `/opt/stonk-ai/generate_narratives_llm_batched.py`.
- ✅ Uses `openrouter/moonshotai/kimi-k2.6` via `openclaw infer` with `timeout -s KILL` wrapper.
- ✅ Generates 11 holdings + 20 watchlist symbols in ~7 minutes, batched 6 symbols per call.
- ✅ Outputs written to `/var/www/hedge-fund-website/popup_narratives.json` and `/var/www/hedge-fund-website/watchlist_narratives_llm.json`.
- ✅ `company_knowledge.json` cache present on VPS and auto-extends for new symbols.
- ✅ `stonk-ai-llm-narrative.timer` enabled and running every 15 minutes on VPS.
- ✅ Merged `popup_content.json` and `watchlist_narratives.json` contain LLM narrative fields (verified AFRM, AMD).

## Why OpenRouter won
- DeepInfra `deepinfra/moonshotai/Kimi-K2.6` was flaky/slow for batched prompts; `openrouter/moonshotai/kimi-k2.6` returns valid JSON reliably in ~2 min per 6-symbol batch.
- Running as `root` avoids OpenClaw auth-profile issues (auth profiles live under `/root/.openclaw/`).

## Operational cadence
- LLM generation: every 15 minutes.
- v6 merge (data + LLM overlay): every 2 minutes.
- Worst-case narrative latency: ~15 minutes.

## Files on VPS
- `/opt/stonk-ai/generate_narratives_llm_batched.py`
- `/opt/stonk-ai/stonk-ai-llm-narrative.service`
- `/opt/stonk-ai/stonk-ai-llm-narrative.timer`
- `/opt/stonk-ai/company_knowledge.json`
- `/var/www/hedge-fund-website/popup_narratives.json`
- `/var/www/hedge-fund-website/watchlist_narratives_llm.json`

## Health monitor update
- Added `check_llm_narrative_pipeline()` to `/opt/stonk-ai/comprehensive_monitor.py`.
- New check verifies:
  - `stonk-ai-llm-narrative.timer` is active.
  - `stonk-ai-llm-narrative.service` is not in failed state.
  - `popup_narratives.json` and `watchlist_narratives_llm.json` exist, are valid JSON, and contain expected fields.
  - Merged `popup_content.json` and `watchlist_narratives.json` carry LLM narrative fields.
  - LLM output timestamps are fresh (within 25 min).
- Scheduled `comprehensive_monitor.py` in stonkai crontab:
  - Every 5 minutes during US market hours (09:00–16:59 ET, Mon–Fri).
  - Every 15 minutes pre/post-market and weekends.
  - Logs to `/opt/stonk-ai/logs/comprehensive_monitor.log`.

## Backup + handover + commit
- Comprehensive VPS backup created: `/opt/stonk-ai/backups/comprehensive-20260701-0340.tar.gz` (51 MB)
- Key files also in `/opt/stonk-ai/backups/comprehensive-20260701-0340/extra/`
- Handover note sent to Einstein: `/opt/stonk-ai/agent-messages/jeeves/handover-jeeves-20260701.md`
- Local workspace committed: `2026-07-01: VPS LLM narrative pipeline + health monitor check` (`763febc`)

## Post-audit fixes (2026-07-01 12:02 HKT)
- Full end-to-end audit of trading bot, holdings/watchlist pipelines, and Alpaca connectivity.
- Fixed empty `portfolio_state.json` by routing consumers to `portfolio_data.json`.
- Fixed LLM watchlist narrative output structure bug in `generate_narratives_llm_batched.py`; regenerated all 20 watchlist narratives.
- Hardened health monitor against malformed LLM output and moved semantic-contradiction noise to warnings.
- Disabled broken `held_news_topup.py` cron (broken import + deprecated Finnhub).
- Post-fix backup: `/opt/stonk-ai/backups/comprehensive-20260701-0407.tar.gz`.
- Handover sent to Einstein: `/opt/stonk-ai/agent-messages/jeeves/handover-jeeves-20260701-audit.md`.
- Final health status: HEALTHY; all data files fresh; no failed systemd units.

## Tier rename + buy queue (2026-07-01 12:46 HKT)
- Decided to keep backend tiers as `STRONG_NOW` / `NOW` for safety, but expose user-friendly labels:
  - `STRONG_NOW` → **STRONG**
  - `NOW` → **ACTIVE**
- Added `display_tier` to `ai_watchlist_live.json` via `dynamic_watchlist_manager.py`.
- Added `buy_candidates` to `ai_watchlist_live.json` with statuses:
  - `queued` — new buy
  - `add` — held, underweight, still eligible
  - `hold` — held, at/above target weight
  - `not_ready` — readiness/confirmations not met
  - `tier_too_low` / `no_price` — other blockers
- Computed position weights from `portfolio_data.json` to distinguish `add` vs `hold`.
- Patched `index.html`:
  - Watchlist table shows `STRONG`, `ACTIVE`, `WATCH`, `MONITOR` badges.
  - New **"Next Buys"** section above the watchlist table showing queued, held, and blocked symbols with reasons.
  - Fetches `buy_candidates` from `ai_watchlist_live.json` into `window.buyCandidates`.
- Validated patched `index.html` with `node --check` (exit 0).
- Re-ran watchlist manager; verified `buy_candidates` and `display_tier` present.

## Buy queue with add/hold distinction (2026-07-01 13:05 HKT)
- Enhanced `dynamic_watchlist_manager.py` to load position weights from `portfolio_data.json`.
- Replaced generic `held` status with:
  - `add` — held, underweight (< 6%), still entry-eligible
  - `hold` — held, at/above threshold or no add signal
- Updated frontend "Next Buys" section to show:
  - 🎯 New buys
  - ➕ Add to position
  - ✓ Hold
  - 🚫 Not ready
- Updated v6 merge script to inject accurate bot-status suffix into `whatTriggersBuy` for all statuses (queued, add, hold, not_ready, tier_too_low, no_price).
- Updated About/FAQ tier narrative to explain tiers signal strength, while the Next Buys queue shows actual bot intent.
- Verified end-to-end: timers active, files fresh, LLM outputs valid, health monitor HEALTHY, website serving new labels.
- Backup: `/opt/stonk-ai/backups/comprehensive-20260701-0513.tar.gz`.

## VPS memory maintenance (2026-07-01 13:20 HKT)
- Created `/opt/stonk-ai/vps_memory_maintenance.py` to analyze memory files on the VPS.
- Runs daily at 3 AM HKT via root cron.
- Uses `openclaw infer model run --model openrouter/moonshotai/kimi-k2.6`.
- Writes report to `/opt/stonk-ai/jeeves-memory/DREAMS.md`.
- Does **not** auto-modify `MEMORY.md` (report-only by default; `--apply` available).
- First report identified stale DeepInfra model references, Alpaca Premium Data backtest degradation, and paper→live transition as key items to track.
- Backup: `/opt/stonk-ai/backups/comprehensive-20260701-0520.tar.gz`.

## Options skew signal analysis (2026-07-01 14:05 HKT)
- Howie asked about optimizing StonkBOT with additional Alpaca Premium Data feeds.
- Recommended using existing options IV data rather than adding website dashboards.
- Created `/opt/stonk-ai/analyze_options_skew_signal.py` to correlate existing IV metrics (`iv_30d`, `iv_skew`, `iv_rank`) with trade outcomes.
- Scheduled daily at 7 AM HKT via root cron.
- First run on 12 matched trades showed:
  - `iv_skew` vs P&L%: **-0.61** (higher put/call skew = worse outcomes)
  - `iv_30d` vs P&L%: **-0.34**
- Sample size too small to act on; will accumulate more data before any readiness-score change.
- No website changes made.
- Handover to Einstein: `/opt/stonk-ai/agent-messages/jeeves/handover-jeeves-20260701-options-skew-analysis.md`

## Health monitor consolidation (2026-07-01 14:36 HKT)
- Reduced `comprehensive_monitor.py` cadence from 5 min market hours / 15 min off-hours to every 15 minutes around the clock.
- Added `/opt/stonk-ai/heartbeat_tracker.py` and heartbeat recording to all critical cron jobs.
- Added `check_cron_heartbeats()` to `comprehensive_monitor.py` to detect stale/missing scheduler runs.
- Kept `stonk_health_check.py` as lightweight public-status writer; no website changes.
- Backup: `/opt/stonk-ai/backups/comprehensive-20260701-0636.tar.gz`.
- Handover to Einstein: `/opt/stonk-ai/agent-messages/jeeves/handover-jeeves-20260701-health-monitor-consolidation.md`

## Readiness threshold UI alignment (2026-07-01 15:30 HKT)
- Howie noticed website still showed old entry gate (readiness ≥72, 2-3 confirmations).
- Updated `/var/www/hedge-fund-website/index.html` to match current backend gate:
  - readiness ≥75 + 4+ confirmations + above EMA
- Updated:
  - Trade log bot rationale
  - Readiness tooltip thresholds
  - Watchlist tier tooltip labels (STRONG / ACTIVE / WATCH / MONITOR)
  - Watchlist popup fallback text
- Verified zero stale "72" references in readiness context.
- `node --check` passed; v6 merge triggered.
- No backend logic changes.
- Handover to Einstein: `/opt/stonk-ai/agent-messages/jeeves/handover-jeeves-20260701-readiness-thresholds.md`

## End-to-end tier / readiness alignment (2026-07-01 15:45 HKT)
- Found mismatch: backend entry gate requires readiness ≥75 + 4 confirmations + above EMA, but `display_tier` mapped backend readiness bands directly, so symbols like MU (readiness 82, conf 3) showed `STRONG` despite not being entry-eligible.
- Updated `dynamic_watchlist_manager.py` so `display_tier` reflects actual entry eligibility:
  - `STRONG`: `STRONG_NOW` + entry-eligible
  - `ACTIVE`: `NOW` + entry-eligible
  - `WATCH`: strong/active/watch signal but not entry-eligible
  - `MONITOR`: weak/tracking
- Updated `buy_candidates` display tier to use same logic.
- Updated watchlist tier tooltip in `index.html` to explain the new semantics.
- Re-ran watchlist manager; `ai_watchlist_live.json` now aligned.
- Backend `signal_engine.py` tier assignment unchanged; only display/action tier changed.
- Backup: `/opt/stonk-ai/backups/comprehensive-20260701-0746.tar.gz`.
- Handover to Einstein: `/opt/stonk-ai/agent-messages/jeeves/handover-jeeves-20260701-end-to-end-tier-alignment.md`

## Watchlist popup / table tier label sync (2026-07-01 15:50 HKT)
- Howie pointed out TER still showed `NOW` under readiness in the popup instead of `ACTIVE`.
- Found that holdings popup, watchlist table badge, and mobile watchlist popup were deriving tier labels from backend `signal_tier` directly rather than `display_tier`.
- Patched all three places to use `display_tier` from `ai_watchlist_live.json` when available, falling back to backend mapping.
- Now TER shows `ACTIVE` consistently across table badge, readiness chip, and popup.
- `node --check` passed; v6 merge triggered.
- Backup: `/opt/stonk-ai/backups/comprehensive-20260701-0802.tar.gz`.

## Next Buys UI cleanup (2026-07-01 16:15 HKT)
- Howie noted "Next Buys" should only show symbols the bot will actually buy.
- Updated frontend to show only:
  - 🎯 New buys
  - ➕ Add to position
- Moved held and not-ready counts to a small, muted transparency line below.
- Removed verbose hold/not-ready lists from the main Next Buys box.
- `node --check` passed.
- Backup: `/opt/stonk-ai/backups/comprehensive-20260701-0812.tar.gz`.

## Next Buys UI final (2026-07-01 16:17 HKT)
- Howie asked to remove even the muted held/not-ready transparency line.
- "Next Buys" section now appears **only** when there are actual new buys or add-to-position candidates.
- No mention of held or not-ready symbols in that area at all.
- `node --check` passed.
- Backup: `/opt/stonk-ai/backups/comprehensive-20260701-0817.tar.gz`.

## Tier semantics: signal strength vs action (2026-07-01 16:22 HKT)
- Howie pointed out it was confusing to see TER (`ACTIVE`, readiness 77) as a buy candidate while MU (`WATCH`, readiness 82) was not.
- Reverted `display_tier` to map purely by backend signal-strength bands:
  - `STRONG_NOW` → **STRONG** (readiness ≥78)
  - `NOW` → **ACTIVE** (readiness 72–77)
  - `WATCH` → **WATCH** (readiness 55–71)
  - `MONITOR`/`TRACKING` → **MONITOR**
- Updated watchlist tier tooltip to explain tiers as signal-strength bands only.
- The **Next Buys** queue remains the only place showing actual bot intent (new buys / add to position).
- This cleanly separates signal strength from action.
- Backup: `/opt/stonk-ai/backups/comprehensive-20260701-0821.tar.gz`.

## Next Buys queue split (2026-07-01 16:27 HKT)
- Howie found it confusing that TER (ACTIVE tier) appeared in "Next Buys" while stronger STRONG-tier stocks were held but not being bought.
- Clarified the distinction: tiers = signal strength; queue = actual action.
- Split the queue into:
  - 🎯 New Buys — not held, entry-eligible
  - ➕ Top up holdings — held, underweight, entry-eligible
- Sorted each group by readiness score descending so highest-conviction symbols appear first.
- Added readiness score (Rxx.x) to each queue entry for full transparency.
- Backup: `/opt/stonk-ai/backups/comprehensive-20260701-0825.tar.gz`.

## Next Buys simplified (2026-07-01 16:34 HKT)
- Howie wanted the Next Buys queue to show only ticker symbols, no extra metadata.
- Updated frontend so "Next Buys" now displays:
  - 🎯 New buys: comma-separated tickers
  - ➕ Top up holdings: comma-separated tickers
- Removed tier, weight, and readiness score from the queue line.
- Details remain available by clicking the ticker in the watchlist table or popup.
- Backup: `/opt/stonk-ai/backups/comprehensive-20260701-0834.tar.gz`.

## Paper rebalancer prototype (2026-07-01 16:51 HKT)
- Howie wants trading bot to use readiness scores more directly for capital allocation.
- Built `/opt/stonk-ai/paper_rebalancer.py` as a paper-only prototype.
- Computes target weights proportional to readiness for entry-eligible symbols.
- Example current plan deploys ~90% of portfolio across 5 symbols (SOFI, AMD, UPST, TER, LRCX), each ~18%.
- This contrasts sharply with the real bot, which holds 62% cash and adds only small underweight positions.
- Scheduled every 15 min during market hours; does not execute trades.
- Will collect plans for comparison before any live bot changes.
- Handover to Einstein: `/opt/stonk-ai/agent-messages/jeeves/handover-jeeves-20260701-paper-rebalancer.md`
- Backup: `/opt/stonk-ai/backups/comprehensive-20260701-0851.tar.gz`.

## Paper rebalancer with simulated P&L (2026-07-01 16:55 HKT)
- Extended paper rebalancer to track daily simulated returns.
- Writes history to `/opt/stonk-ai/paper_rebalance_history.json`.
- Each run records target weights, prices, and computes the simulated daily return from the previous day's plan.
- Starting 2026-07-02, will accumulate a paper track record vs. the real bot.
- Backup: `/opt/stonk-ai/backups/comprehensive-20260701-0903.tar.gz`.

## Tier labels: PRIME / BUILDING / WATCHING / TRACKING (2026-07-01 17:17 HKT)
- Howie wanted retail-friendly, non-action tier names.
- Replaced frontend display labels end-to-end:
  - STRONG → **PRIME** (readiness ≥78)
  - ACTIVE → **BUILDING** (readiness 72–77)
  - WATCH → **WATCHING** (readiness 55–71)
  - MONITOR → **TRACKING** (<55)
- Updated:
  - Watchlist table badges
  - Holdings popup readiness sub-label
  - Mobile watchlist popup
  - Watchlist tier tooltip
  - Readiness tooltip labels
  - About/FAQ "How does the AI work?" section
  - `dynamic_watchlist_manager.py` `DISPLAY_TIER` mapping
  - v6 merge script mapping
- Backend `STRONG_NOW` / `NOW` / `WATCH` / `MONITOR` remain unchanged.
- Tooltip now explicitly states: "Tiers show signal strength, not bot action. Next Buys shows what the bot is actually doing."
- Note: user screenshots showed stale labels and wrong colors. Multiple issues found and fixed:
  - `dynamic_watchlist_manager.py` still mapped to old labels — fixed.
  - Second `tierColors` JS mapping (watchlist table) had old labels and wrong colors — fixed:
    - `STRONG_NOW` label: `STRONG` → `PRIME`
    - `NOW` color: `#ef4444` (red) → `#22c55e` (green)
    - `MONITOR` label: `MONITOR` → `TRACKING`
  - `WATCHING` readinessTag mapping fixed.
  - `generate_popup_content_narrative_v6_server.py` only set `display_tier` for symbols in `buy_candidates`. Fixed to ensure every watchlist symbol gets a `display_tier` from tier mapping.
  - **Cache buster** was stuck on `v133-minimal` from June 29. Bumped to `v20260701-1740-v145-tier-rename` with new localStorage key `stonkbot_v139`. Forces returning visitors to reload fresh.
- Restored `index.html` from pre-tier-rename backup (2026-07-01 08:42) and applied a clean patch to avoid accumulated errors.
- Force-refresh or visit `https://stonkbot.ai/?v=20260701-1740-v145-tier-rename` directly.
- Backup: `/opt/stonk-ai/backups/comprehensive-20260701-0940.tar.gz`.

## Tier labels follow-up (2026-07-01 17:46 HKT)
- User still saw wrong colors / "black fonts" after cache-buster fix.
- Root cause identified: watchlist table row background for `NOW`/`BUILDING` tier was still tinted red (`rgba(239,68,68,0.05)`), making the row look visually wrong and causing the "black" appearance against the dark navy background.
- Fixed:
  - `NOW`/`BUILDING` row background: `rgba(239,68,68,0.05)` (red) → `rgba(34,197,94,0.05)` (green).
  - Confirmed no other red `NOW`/`BUILDING` references remain.
  - Bumped cache buster again to `v20260701-1750-v146-tier-rename` with localStorage key `stonkbot_v140`.
- Verified:
  - `node --check` passes.
  - `watchlist_narratives.json` has no missing `display_tier` values.
  - Live HTML row background is now green for `BUILDING`.
- Try directly: **https://stonkbot.ai/?v=20260701-1750-v146-tier-rename**
- Backup: `/opt/stonk-ai/backups/comprehensive-20260701-0945.tar.gz`.

## Front-end auto-refresh for watchlist (2026-07-01 18:00 HKT)
- Howie approved adding front-end auto-refresh as the first improvement.
- Added 30-second polling for the inline watchlist tab while it's active:
  - Re-fetches `ai_watchlist_live.json` every 30s.
  - Re-renders watchlist table and Next Buys queue.
  - Preserves current sort selection (`currentWatchlistSort`).
  - Skips refresh if the watchlist modal is open (modal already has its own 30s polling).
- Modified `renderWatchlistInline()` to support re-renders without destroying the container.
- Bumped cache buster to `v20260701-1800-v147-watchlist-poll` / localStorage key `stonkbot_v141`.
- Verified `node --check` passes.
- Direct URL: **https://stonkbot.ai/?v=20260701-1800-v147-watchlist-poll**
- Backup: `/opt/stonk-ai/backups/comprehensive-20260701-0955.tar.gz`.

## Extended-hours popup flicker fix (2026-07-01 18:15 HKT)
- Howie noticed extended-hours prices in popups intermittently appearing/disappearing.
- Root causes identified:
  1. Alpaca SIP extended-hours 5-min bars are sparse for some symbols.
  2. `fetch_ai_watchlist.py` overwrote existing pre-market/after-hours values with `None` when a fresh fetch returned no bar data.
  3. The popup reads directly from `ai_watchlist_live.json`, so it inherited the intermittent data.
- Fix implemented:
  - Patched `alpaca_data.py::get_extended_hours_bars()` to add a snapshot-based fallback:
    - If during pre-market window (08:00–13:25 UTC) and no pre-market bars, compute `premarket_change_pct` from `(snapshot_price - prev_close) / prev_close`.
    - If during after-hours window (20:00–23:59 UTC) and no after-hours bars, compute `afterhours_change_pct` from the same snapshot-vs-prev_close formula.
  - Patched `fetch_ai_watchlist.py` to skip overwriting real/extended data with `None`/`''`.
  - Restarted `stonk-ai-watchlist.service` to load updated code.
- Verified:
  - `python3 -m py_compile` passes for both files.
  - `get_extended_hours_bars()` returns stable pre-market/after-hours values for sample symbols.
  - Fresh `ai_watchlist_live.json` shows consistent pre-market values across symbols.
- Note: during regular market hours, after-hours field is intentionally absent; it appears only during the after-hours session.
- Backup: `/opt/stonk-ai/backups/comprehensive-20260701-1000.tar.gz`.

## LLM cost-saving experiment (2026-07-01 18:30–18:45 HKT)
- Howie asked whether to reduce LLM narrative refresh rate or use a cheaper model. Recommendation: cheaper model first.
- **Test 1 — `openrouter/google/gemini-flash-1.5`:** Failed immediately with OpenRouter 403/ToS violation. Not usable in this setup.
- **Test 2 — `openrouter/meta-llama/llama-3.1-8b-instruct`:** Worked for a single-symbol prompt and produced acceptable output.
- **Test 3 — `openrouter/meta-llama/llama-3.1-70b-instruct`:** Full generation test run.
  - Watchlist: 20/20 narratives generated successfully.
  - Holdings: only 6/11 generated; second batch returned no usable symbol-keyed output. Quality was acceptable but parsing reliability is not production-ready.
- **Decision:** Reverted narrative generator to `openrouter/moonshotai/kimi-k2.6` and re-ran to restore all 11 holdings. Site is back to full Kimi narratives.
- **Next steps if Howie still wants savings:**
  - Add a retry + structured-output parsing pass for Llama 70B/8B before switching.
  - Or test other cheap reliable models (e.g., `openrouter/deepseek/deepseek-chat`, `openrouter/qwen/qwen-2.5-7b-instruct` with output token fix).
  - Keep 15-minute cadence; reducing refresh rate hurts UX more than it saves money.
- Backup: `/opt/stonk-ai/backups/comprehensive-20260701-1015.tar.gz`.

## Smart LLM narrative scheduler (2026-07-01 19:25 HKT)
- Howie approved tiered refresh + weekend/holiday pause.
- Implemented `/opt/stonk-ai/llm_narrative_scheduler.py`:
  - Queries Alpaca `/v2/clock` before each run.
  - **Market open** (Mon–Fri 09:30–16:00 ET): runs generator every 15 min.
  - **Overnight / pre-market** (market closed, next open ≤14h away): runs once per 60 min.
  - **Weekends / public holidays / early market closures** (next open >14h away): skips.
  - Fails open: if Alpaca clock call fails, it runs the generator anyway.
- Updated `stonk-ai-llm-narrative.service` to call the scheduler instead of the generator directly.
- The timer stays at every 15 min; the scheduler decides whether to actually generate.
- Verified: scheduler syntax OK; test run triggered generator during off-hours.
- Expected cost reduction:
  - Flat 15-min: ~$83/month.
  - Smart schedule: ~$20–25/month.
- Backup: `/opt/stonk-ai/backups/comprehensive-20260701-1045.tar.gz`.

## Fix: Next Buys weight denominator bug (2026-07-01 19:55 HKT)
- During the SOFI deep-dive, I spotted that `dynamic_watchlist_manager.py` computed position weights using only **deployed equity** (sum of position market values) instead of **total portfolio value** (cash + equity from `account.portfolio_value`).
- This made every position look ~2.6× more concentrated than reality. Result: AMD/UPST/LRCX were incorrectly flagged as **hold** when they were actually underweight and should have been **add**.
- Fix: updated the portfolio-value lookup to prefer `portfolio["account"]["portfolio_value"]` before falling back to legacy `total_value` / `portfolio_value` fields.
- Regenerated `ai_watchlist_live.json`.
- Result:
  - **Add:** AMD (5.75%), UPST (3.38%), LRCX (3.82%), SOFI (0.96%), TER (1.43%)
  - **Hold:** MRVL (2.63%, only 3 confirmations), HOOD (2.41%, only 3 confirmations)
  - **Queued:** LMND
- MRVL/HOOD remain hold not because of weight but because they have only 3 confirmations (entry gate requires ≥4).
- Backup: `/opt/stonk-ai/backups/comprehensive-20260701-1055.tar.gz`.

## Note
- Mac-side generation pipeline is no longer needed; everything runs on the VPS.
- VPS memory maintenance is report-only; Mac remains source of truth for `MEMORY.md`.


