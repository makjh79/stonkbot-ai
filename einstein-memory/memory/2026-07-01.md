# 2026-07-01 — Auto-Dream installed, up to speed with Jeeves

## Auto-Dream memory maintenance
- Installed daily cron at 03:00 UTC (11:00 HKT), isolated session
- Set up skill at `skills/auto-dream/SKILL.md` with 5-phase dream prompt
- Created `memory/dream-log.json` and archive directory
- Manual trigger "Dream now" tried — timed out after 5 min on OpenRouter Kimi K2.6
- **Issue:** OpenRouter Kimi K2.6 intermittently fails in isolated sessions (also Pipeline Monitor cron). Decision: monitor before switching to DeepInfra.
- Auto-archived 18 old daily files from June to `memory/archive/2026-06/`

## Jeeves catch-up (July 1 work)
### LLM pipeline on VPS
- Batched LLM generator (`generate_narratives_llm_batched.py`) runs every 15 min with OpenRouter Kimi K2.6
- v6 merge every 2 min overlays LLM narratives on popup/watchlist data
- Uses `company_knowledge.json` cache, latency ~15 min worst case

### Frontend updates
- Tier rename: backend `STRONG_NOW`/`NOW` → frontend `STRONG`/`ACTIVE` (no blast radius)
- New **buy queue** in `ai_watchlist_live.json`: `queued`, `add`, `hold`, `not_ready`, `tier_too_low`, `no_price`
- Frontend "Next Buys" section shows 🎯/➕/✓/🚫 with tier display labels

### Health monitor consolidation (14:36 HKT)
- comprehensive_monitor.py reduced to every 15 min all the time (was 5 min market hours)
- New heartbeat tracker (`heartbeat_tracker.py`) — 18 jobs tracked
- Monitor checks for stale/missing cron heartbeats

### Options skew analysis (14:05 HKT)
- New script `analyze_options_skew_signal.py`: correlates existing IV metrics with trade outcomes
- First results (12 trades): `iv_skew` vs P&L = -0.61; `iv_30d` vs P&L = -0.34
- High put skew → worse outcomes (fear/falling knife signal?)
- Not enough data to act yet. Runs daily 7 AM HKT.

### Jeeves VPS memory maintenance
- Report-only script, runs 3 AM HKT, writes to `DREAMS.md`
- First run correctly identified DeepInfra config confusion, premium data backtest degradation, paper→live pending

## Key decisions noted
- Jeeves runs Ollama locally (`kimi-k2.7-code:cloud` primary), not DeepInfra
- Premium data backtest: +102% → +25.5%, Sharpe 1.30 → 0.52 — **needs investigation before live**
- Options skew signal directionally interesting but needs 50+ trades to validate
