# 2026-06-25 — Jeeves Summary (synced from jeeves-memory)

## Signal Update Service Fix
- Health monitor flagged DEGRADED — all watchlist positions in TRACKING, no scored signals.
- `stonk-ai-signal-update.service` had failed.
- Root cause: `update_signal_performance.py` and `export_signals_for_website.py` imported module-level functions (`load_signal_history`, `save_signal_history`, `get_signal_stats`, `export_for_website`) from `signal_tracker.py` — but those only existed as class methods on `SignalTracker`.
- Secondary issue: `signals.json` schema mismatch — signal engine writes momentum/quality scores (no `status`/`outcome`), but `SignalTracker` expects tracked signals with `status`, `outcome`, `entry_price`. Caused `KeyError: 'status'`.

## Fix Applied
1. Added compatibility wrapper functions to `signal_tracker.py`.
2. Made `SignalTracker` class methods robust to mixed-format signals (`.get('status')` instead of `['status']`, filtering to only tracked signals).
3. Re-enabled service and 5-minute timer.

## Note
- The "all TRACKING" alert was stale — main trading bot was running fine with 11 signal candidates (AMAT, AFRM, AMD in NOW tier). Signal-update service just couldn't track performance due to import errors.
- VPS disk 17%, uptime 24 days, all other services nominal.