# 2026-06-28 — Major Risk Engine + Data Pipeline Overhaul

## What happened
Howie asked for a full summary of the trading bot, then shared Gemini's audit of the system. Gemini identified: overfitting, collinearity, negative expectancy (avg loser > avg winner), LCID position cap breach. Howie asked me to audit and fix.

## Risk engine fixes
1. **ATR stops** — replaced fixed 10% hard stop with 1.5× ATR (clamped 3-8%), trailing with 2.0× ATR (clamped 3-10%). Low-vol stocks get tighter stops, high-vol stocks slightly wider but capped.
2. **STRONG_NOW cap override removed** — was 12% for STRONG_NOW, 8% for others. Now 8% for all tiers. Sizing still 1.5× for STRONG_NOW via multiplier.
3. **Concentration trim trigger** — lowered from 1.25× to 1.0×. Trims at 8% cap, not 10%.
4. **Backtest re-run**: Sharpe 1.30→0.89, return +101.97%→+56.85%, max DD -31.7%→-29.5%. Drop is expected/healthy — old number was inflated by wide stops in bull market.

## Data pipeline cleanup
1. **`feed=sip`** added to ALL Alpaca API calls — snapshots, quotes, trades, bars. Was missing on snapshot and quote endpoints, causing DOCN and other symbols to return no price data.
2. **Yahoo Finance RSI removed** — `get_rsi_from_yahoo()`, `fetch_yahoo_history()` stubbed. All RSI from Alpaca.
3. **Polygon RSI removed** — `get_rsi_from_polygon()` stubbed.
4. **Finnhub removed** — `fetch_earnings()`, `fetch_recommendation()`, `fetch_news()` all stubbed in `signal_enricher.py`. Only Alpaca news API used.
5. **PEAD factor dropped** — Alpaca has no earnings data API. Factor removed entirely. 5% weight redistributed to sector strength (25%→30%).
6. **Zero external data dependencies** — everything from Alpaca only.

## Signal engine fixes
1. **MR merge bug** — mean reversion signals were replacing momentum signals entirely, losing avg_volume, sector, earnings, news data. Fixed to merge fields instead.
2. **Min bars lowered** — 50→20. Newer symbols with limited history now get scored correctly.
3. **DOCN now has volume data** — was returning None due to 50-bar minimum + missing feed=sip.

## Popup narrative rewrite
1. 9 narrative functions rewritten with trader-style commentary (not debug logs).
2. ~45 hardcoded company profiles (AAPL, NVDA, MU, etc.) + dynamic fallback from signal data for unknown symbols.
3. Confirmed/missing factor lists added to `whyOnWatchlist` and `whatTriggersBuy`.
4. Factor chips compacted — separator dots removed, gaps tightened.
5. PEAD chip removed from HTML (9→8 factors).

## Files changed
- `risk_engine.py` — ATR stops, concentration trigger, hard cap fields
- `trading_bot.py` — STRONG_NOW cap override removed
- `readiness_score.py` — 8-factor weights, PEAD removed, sector 30%
- `signal_engine.py` — PEAD removed, MR merge fix, min bars 20
- `generate_popup_content.py` — narrative rewrite, label cleanup
- `fetch_ai_watchlist.py` — feed=sip on all 4 API calls, Yahoo/Polygon stubbed
- `alpaca_data.py` — feed=sip on snapshots and quotes
- `pead_factor.py` — stubbed
- `signal_enricher.py` — fetch_earnings/recommendation/news stubbed
- `index.html` — PEAD chip removed, /9→/8, factor chips compacted, DOCN tier fix

## Backups
- `comprehensive-20260628-0650.tar.gz` — after ATR stops
- `comprehensive-20260628-0830.tar.gz` — after all changes (498KB, 203 files)
- `risk-fix-20260628/` — individual file backups

## Key decisions
- **Freeze rule**: No parameter changes for 200 live trades. Let ATR stops prove themselves.
- **Sector 30%**: Best non-price predictor (+0.459 corr). Given PEAD's 5% share.
- **PEAD dropped**: 57% coverage, 2.85% effective contribution. Not worth an external dependency.
- **Honest assessment**: Bot architecturally sound but needs 200 trades to validate. Live expectancy was -1.62%/trade with old stops. ATR stops should fix by tightening average losers.
## June 28 Session Updates (continued)

### Pipeline Monitor
- Set up round-the-clock health monitoring via OpenClaw cron (every 10 min)
- `vps_check.py` on VPS reads existing `health_status.json` + `monitor_status.json` instead of re-checking
- Consolidated 4 redundant health check scripts — one source of truth
- Alerts Howie on Telegram ONLY when broken, silent when healthy
- Added data integrity checks: factor denominator (/9 vs /8), ghost earnings_confirmed field
- Weekend-aware: skips freshness/content checks when market closed

### Factor System Fixes
- Fixed `/9` → `/8` in 3 files: `generate_popup_content.py`, `dynamic_watchlist_manager.py`, `readiness_score.py`
- Removed `earnings_confirmed` ghost field from `readiness_score.py` confirmations dict
- Fixed `stonk_health_check.py` watchlist expectation 15 → 20 symbols
- Added weekend skip for signals freshness check in `stonk_health_check.py`
- Fixed factor chip count: HTML now counts green chips dynamically instead of trusting server's `confirmation_count`
- Fixed MOM chip: uses `preGenerated.momentumScore` (real 0-100 score) instead of `confirmations.momentum_score` (weighted readiness component)
- Passed `momentumScore` as 3rd parameter to `buildFactorChips(conf, count, momentumScore)`

### Model Config
- DeepSeek V4 Flash added to `models.providers.deepinfra.models` in openclaw.json
- Set as config default (`agents.defaults.model.primary`)
- New DeepInfra API key configured
- Image model switched: GLM-4.5V (404) → Qwen/Qwen3-VL-30B-A3B-Instruct
- Known issue: DeepSeek rate-limited, falls back to GLM-5.2

### Backups
- `/opt/stonk-ai/backups/pre-factor-fix-20260628/` — 7 files backed up before changes

### Practice
- Howie requested backups before any VPS changes going forward — noted
