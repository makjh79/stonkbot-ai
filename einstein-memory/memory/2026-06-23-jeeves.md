# 2026-06-23 — Jeeves Summary (synced from jeeves-memory)

## Performance Chart Fix
- `portfolio_history.json` was truncated to only 2026-06-22 micro-checks (500 entries, one day). Page needs ≥2 daily snapshots.
- Root cause: `fetch_data_simple.py::update_history()` kept only 500 most recent checks; one day with many micro-checks wiped all prior days.
- Restored 19 daily snapshots from backup, merged live data + SPY benchmark.
- Patched to keep one snapshot/day (closest to noon UTC), rolling 180-day window.

## Safeguards
- History-length health check in `health_check.py`: flags DEGRADED if <7 unique days. Every 5 min via cron.
- Nightly reconstruct cron at 22:30 UTC via `reconstruct_portfolio_history.py` with flock.

## Watchlist Sentiment
- Sentiment files were ~57 hours stale (hourly cron was commented out). Re-enabled at `*/15 * * * *`.
- Rewrote `generate_sentiment.py` to use Finnhub News + Alpaca News API, merge/dedupe, same lexicon scoring.

## Emoji Cleanup / UI Polish
- Audited index.html emoji usage (215 occurrences, 58 unique). Removed meme/decorative emojis from FAQ, headings, brand label, PL badges, modals.
- Desktop nav: Performance changed from 📊 to 📈. Replaced removed emojis with inline SVG line icons.

## Tooling
- Installed Playwright on VPS for automated screenshots and visual testing.

## Redesign Attempt (rolled back)
- Attempted tabbed single-page redesign (v43-v49) — broke on user's mobile browser. Rolled back to v42.
- Lesson: large HTML structural changes via string replacement are risky — can silently swallow adjacent elements.

## Redesign Continuation (late evening)
- Re-implemented incrementally: v50 (added header bar) → v51 (removed old nav). Tested on real device at each step.